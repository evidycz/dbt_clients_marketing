version: 2

sources:
  - name: glami
    database: "{{ var('marketing_database').get(target.name, target.database) }}"
    schema: "{{ var('client_name', 'client') }}_glami"

    loader: DLT

    tables:
      - name: categories