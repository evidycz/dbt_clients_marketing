version: 2

sources:
  - name: google_analytics
    database: "{{ var('raw_database', 'gcp-raw-data') }}"
    schema: "{{ var('client_name', 'client') }}_google_analytics"

    loader: DLT

    tables:
      - name: events
      - name: items
      - name: sources