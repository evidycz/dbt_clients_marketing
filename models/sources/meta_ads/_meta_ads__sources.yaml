version: 2

sources:
  - name: meta_ads
    database: "{{ var('raw_database', 'gcp-raw-data') }}"
    schema: "{{ var('client_name', 'client') }}_meta_ads"

    loader: DLT

    tables:
      - name: campaigns
      - name: campaigns__action_values
      - name: campaigns__actions