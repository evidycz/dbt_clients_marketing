version: 2

sources:
  - name: seznam_sklik
    database: "{{ var('raw_database', 'gcp-raw-data') }}"
    schema: "{{ var('client_name', 'client') }}_seznam_sklik"

    loader: DLT

    tables:
      - name: campaigns