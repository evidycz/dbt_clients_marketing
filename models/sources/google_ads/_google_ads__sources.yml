version: 2

sources:
  - name: google_ads
    database: "{{ var('marketing_database').get(target.name, target.database) }}"
    schema: "{{ var('client_name', 'client') }}_google_ads"

    loader: DLT

    tables:
      - name: campaigns